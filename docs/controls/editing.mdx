---
title: Change notes and values
sidebar_position: 3
description: Fine, coarse, digit, empty-cell, and list editing behavior.
---

# Change notes and values

NullPerator separates **moving** from **changing**. In tracker grids, directions
move the cursor and <Keycap>Enter</Keycap> plus a direction changes the active
value. In settings lists, horizontal directions make the everyday adjustment
directly, while <Keycap>Enter</Keycap> plus a vertical direction provides the
faster step.

## Shortcuts

<ShortcutReference
  groups={[
    {
      title: 'Tracker grids',
      items: [
        {
          keys: ['Enter'],
          title: 'Initialize an empty cell',
          input: 'Tap Enter',
          detail: 'The value appears on button-down; you do not need to release Enter first.',
        },
        {
          keys: ['Enter', 'Left', 'Right'],
          title: 'Make a fine change',
          input: 'Hold Enter, then press Left or Right',
        },
        {
          keys: ['Enter', 'Up', 'Down'],
          title: 'Make a coarse change',
          input: 'Hold Enter, then press Up or Down',
          detail: 'Applies a field-specific step to whole-field values.',
        },
      ],
    },
    {
      title: 'Lists and effect values',
      items: [
        {
          keys: ['Left', 'Right'],
          title: 'Change a list value',
          input: 'Tap Left or Right',
        },
        {
          keys: ['Enter', 'Up', 'Down'],
          title: 'Use the faster list step',
          input: 'Hold Enter, then press Up or Down',
        },
        {
          keys: ['Enter', 'Left', 'Right'],
          title: 'Choose an effect digit',
          input: 'Hold Enter, then press Left or Right',
        },
        {
          keys: ['Enter', 'Up', 'Down'],
          title: 'Change the selected effect digit',
          input: 'Hold Enter, then press Up or Down',
        },
        {
          keys: ['Enter'],
          title: 'Audition a note',
          input: 'Hold Enter; release it to stop',
        },
      ],
    },
  ]}
/>

## Tracker grids

- Press <Keycap>Enter</Keycap> on an empty resource cell to initialize it.
- Use <KeyCombo keys={['Enter', 'Left']} /> or <KeyCombo keys={['Enter', 'Right']} /> for the fine step.
- Use <KeyCombo keys={['Enter', 'Up']} /> or <KeyCombo keys={['Enter', 'Down']} /> for the coarse step.
- Keep a direction held to repeat.

The coarse amount follows the field. For example, a Chain phrase changes by 16
and its transpose changes by an octave (12 semitones). On a Phrase note the
bottom bar names the two axes **Note** and **Oct** instead of showing numeric
step sizes.

Drum notes use **D01–D12** instead of pitches. Their **±1 / ±10** controls
choose drum sounds and wrap around the kit; the project scale does not apply.
See [Drum](../instruments/drum) for the parameter table and waveform controls.

Pressing <Keycap>Enter</Keycap> fills an empty Song chain reference, Chain phrase
reference, or Phrase note/instrument cell immediately, using the remembered value.
A new Phrase note carries the last explicit instrument with it; this starts at
Instrument 00 in a fresh session. The instrument needs a sound before you can hear it.
Holding Enter does not repeatedly fill the cell, and releasing it does not insert
another value. FX command cells keep their command-selection behavior.

### Cut the current value

<KeyCombo keys={['Enter', 'Option']} /> cuts the current cell. If you press
<Keycap>Enter</Keycap> first on an empty cell, its value appears immediately.
Keep it held and press <Keycap>Option</Keycap> to cut that newly entered value.
You can also adjust it with a direction before pressing Option.

Holding <Keycap>Option</Keycap> first and then pressing <Keycap>Enter</Keycap>
cuts without first filling the cell. On an empty Phrase NOTE cell, this order
inserts Note Off instead. Cutting a populated NOTE clears its paired INS cell too.

Single-cell cut remembers the value for a later <Keycap>Enter</Keycap> press on
an empty cell. For a block of cells, use [Selection and clipboard](selection-clipboard).

### Why another Enter can change a number

A second consecutive <Keycap>Enter</Keycap> press assigns a free Chain number
in Song, a free Phrase number in Chain, or a new instrument slot in Phrase INS.
On a Phrase `TBL` value it assigns a free Table number. This also replaces an
already populated reference; it is not a numeric increment, and does not copy
the old resource's contents. Move the cursor between separate edits if you do
not want to invoke this consecutive-press gesture.

## Four-digit effect values

Phrase and Table effect parameters are four hexadecimal digits. Hold <Keycap>Enter</Keycap> and
press <Keycap>Left</Keycap> / <Keycap>Right</Keycap> to move the digit focus.
<Keycap>Up</Keycap> / <Keycap>Down</Keycap> edits the focused digit. The
command mnemonic remains highlighted while either its mnemonic or its value is
active, and the bottom bar continues to show command help.

## Project, Device, and Instrument lists

For an editable list value:

- <Keycap>Left</Keycap> / <Keycap>Right</Keycap> changes the normal step without a modifier.
- <KeyCombo keys={['Enter', 'Up']} /> or <KeyCombo keys={['Enter', 'Down']} /> changes the coarse step where that value defines one.
- Enumerations wrap when their field is designed as a circular selector.

Scale and Root can still be changed while <Keycap>Enter</Keycap> is held, but they remain
enumerations rather than numeric coarse-step fields. Disabled or unavailable
items do not silently become editable.

When a note can be previewed, keep <Keycap>Enter</Keycap> held to hear it and release <Keycap>Enter</Keycap> to
stop. If normal playback is running, stop it before previewing a single note.
